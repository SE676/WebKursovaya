@model WebKursovaya.Models.Виды_услуг

@{
    ViewData["Title"] = "Редактирование записи";
}

<h1>Редактирование</h1>

<h4>вида услуги</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="Код_вида_услуги" />
            <div class="form-group">
                <label asp-for="Название_вида_услуги" class="control-label"></label>
                <input asp-for="Название_вида_услуги" class="form-control" />
                <span asp-validation-for="Название_вида_услуги" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Сохранить" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index" class="btn btn-dark">Вернуться к списку</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
