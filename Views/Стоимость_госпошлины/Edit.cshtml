@model WebKursovaya.Models.Стоимость_госпошлины

@{
    ViewData["Title"] = "Редактирование записи";
}

<h1>Редактировать</h1>

<h4>запись о стоимости госпошлины</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="Код_стоимости" />
            <div class="form-group">
                <label asp-for="Дата" class="control-label"></label>
                <input asp-for="Дата" class="form-control" />
                <span asp-validation-for="Дата" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Стоимость" class="control-label"></label>
                <input asp-for="Стоимость" class="form-control" />
                <span asp-validation-for="Стоимость" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Сохранить" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index" class="btn btn-dark">Вернуться к списку</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
